# ========================================
# IRAQ ELECTION API - ENVIRONMENT CONFIG
# ========================================

# Server Configuration
PORT=4001
NODE_ENV=production

# Database Configuration (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Railway auto-provides this
DATABASE_URL=postgresql://postgres:password@localhost:5432/iraq_election

# CORS - Allowed Origins (comma-separated)
# For production, list specific domains
# For development, use * for all origins
ALLOWED_ORIGINS=https://your-frontend.com,https://another-domain.com,http://localhost:3000

# Logging Configuration
LOG_LEVEL=info
# Options: error, warn, info, debug

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
# 15 minutes in milliseconds
RATE_LIMIT_MAX=200
# Maximum requests per window

# Database Seeding
SEED_COUNT=7769
# Number of candidates to generate (default: 7769 - realistic for Iraq)

# Optional: Admin Features
ADMIN_SECRET_KEY=your-super-secret-admin-key-change-this
# Used for protected admin endpoints

# Optional: File Upload
MAX_FILE_SIZE=10485760
# 10MB in bytes

# Optional: Email Configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: Redis (for caching)
# REDIS_URL=redis://localhost:6379

# Optional: Analytics
# GOOGLE_ANALYTICS_ID=UA-XXXXX-Y

# Optional: Security
# JWT_SECRET=your-jwt-secret-key
# SESSION_SECRET=your-session-secret

# ========================================
# DEPLOYMENT NOTES:
# ========================================
# 
# Railway:
# - DATABASE_URL is auto-provided
# - Set other vars in Railway dashboard
# 
# Heroku:
# - DATABASE_URL is auto-provided
# - Use: heroku config:set VARIABLE=value
#
# Vercel:
# - Add environment variables in Vercel dashboard
# - Note: PostgreSQL requires persistent connection
#
# Docker:
# - Create .env file with these values
# - Mount as volume or use docker-compose
#
# ========================================
