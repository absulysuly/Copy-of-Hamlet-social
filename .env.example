# Backend base URL (Windsurf gateway or your local API)
VITE_API_BASE_URL=http://localhost:4001/api

# Toggle mock data mode. Set to "false" once real endpoints are ready.
VITE_USE_MOCKS=true

# Optional Gemini key (should remain server-side in production)
GEMINI_API_KEY=
