// Temporary minimal ContactMPForm - will be restored when translations are fixed
import React from 'react';

export const ContactMPForm: React.FC = () => {
    return (
        <div style={{padding: '20px'}}>
            <h3>Contact MP Form</h3>
            <p>This feature is temporarily unavailable while we fix translation issues.</p>
        </div>
    );
};
