{
  "name": "iraq-election-backend-unified",
  "version": "2.0.0",
  "description": "Unified Backend for Iraqi Election Platform - Combines best features from all deployments",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:migrate": "prisma migrate deploy",
    "db:generate": "prisma generate",
    "db:seed": "node scripts/seed-candidates.js",
    "test": "node test-api.js",
    "build": "prisma generate"
  },
  "keywords": ["iraq", "election", "api", "backend", "democracy"],
  "author": "absulysuly",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
